project('btcode', 'c',
    version: '0.0.0',
    default_options: ['buildtype=release',
                      'b_ndebug=if-release'])

btcode_inc_dir = include_directories('include')

subdir('src')
subdir('tools')
subdir('tests')
